Forked from https://github.com/sprain/PHP-MySQL-Session-Handler

This wordpress plugin will cache wordpress sessions in a mysql database

The table it will cache them in is called sessions.

The table will be auto-created when the plugin is activated.